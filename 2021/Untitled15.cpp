#include<stdio.h>

int main(){
    int win,set,games,flag;
    scanf("%d %d %d",&win,&set,&games);
	if(win>set*games) flag=1;
	
        if((((comp)==k) && comp>=k+s) || ((hm/2)>k)){
        printf("YES\n");
    }
    else{
        printf("NO\n");
    }
    return 0;
}
